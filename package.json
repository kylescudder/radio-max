{
  "name": "mp3-stream-server",
  "version": "1.0.0",
  "description": "Looping MP3 streaming server for EuroTruck Simulator",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "bun run server.ts",
    "build": "bun build server.ts --outdir ./dist"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.5.0"
  }
}
