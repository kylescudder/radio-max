# Origin of the radio-max server, e.g. https://radio-max.example.com
# Empty / unset = same origin as the dashboard (use this for local dev when
# the dashboard is served by the same Bun process).
VITE_API_ORIGIN=
